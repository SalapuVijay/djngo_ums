from django.contrib import admin

# Note: Course, Attendance, Result, Message, and Timetable models are
# already registered in the student app's admin.py
# This prevents duplicate registration errors

